import { variant } from 'styled-system';

export const buttonColor = variant({
  key: 'buttonColors',
  prop: 'colors',
});

export const textTag = variant({
  key: 'textTag',
  prop: 'tag',
});

export const tableMode = variant({
  key: 'tableMode',
  prop: 'mode',
});

export const inputMode = variant({
  key: 'inputMode',
  prop: 'mode',
});

export const badgeColor = variant({
  key: 'badgeColors',
  prop: 'colors',
});

export const stepCounterColors = variant({
  key: 'stepCounterColors',
  prop: 'colors',
});
